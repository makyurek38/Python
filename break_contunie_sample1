# 1-100 arasında Python ile Sayı Tahmin Oyunu Yapımı. 
# Kullanıcının girdiği sayı, bilgisayarın tuttuğu sayıdan küçük ise "daha büyük bir sayı giriniz"
# Kullanıcının girdiği sayı, bilgisayarın tuttuğu sayıdan büyük ise "daha küçük bir sayı giriniz"
# sayıyı bulduğunda ise "TEBRİKLER" şeklinde uyarı veren programın python kodunuz yazınız.
from random import randint 

rand=randint(1, 100) 
sayac=0 

while True: 
  sayac+=1 
  sayi=int(input("1 ile 100 arasında değer girin (0 çıkış): ")) 
  if(sayi==0): 
    print("Oyunu İptal Ettiniz") 
    break 
  elif sayi < rand: 
    print("Daha Yüksek Bir Sayı Girin.") 
    continue 
  elif sayi > rand: 
    print("Daha Düşük Bir Sayı Girin.") 
    continue 
  else: 
    print("Rastele seçilen sayı {0}!".format(rand)) 
    print("Tahmin sayınız {0}".format(sayac)) 
